version: '3'

services:

  server:
    build: docker/server
    restart: always
    depends_on:
      - database
    ports:
      - 8080:80
    volumes:
      - ./server:/var/www/html

  database:
    build: docker/database
    restart: always
    volumes:
      - ./database:/var/lib/mysql